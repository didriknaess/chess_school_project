package chess;
import java.util.*;

public class Map {
    // 'h' = hit ship, 's' = ship, 'm' = missed shot, null = empty
    private ArrayList<ArrayList<Piece>> map = new ArrayList<ArrayList<Piece>>();
    public Map() {
    }
}